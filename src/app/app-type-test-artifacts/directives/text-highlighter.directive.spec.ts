import { TextHighlighterDirective } from './text-highlighter.directive';

describe('TextHighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new TextHighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
